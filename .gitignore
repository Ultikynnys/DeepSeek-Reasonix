node_modules/
dist/
coverage/
.stryker-tmp/
.env
.env.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/
*.tsbuildinfo
sessions/
*.jsonl
!tests/**/*.jsonl
.turbo/
# Local-only Claude Code settings — personal overrides, never committed.
.claude/settings.local.json
# Per-user Reasonix state under any .reasonix/ directory is fully ignored.
# Force-add settings.json / skills/ if you ever want them tracked.
# Personal bun lockfile — project uses npm officially.
bun.lock

# Local portable Node/npm used for development on machines without npm.
.tools/
.npm-cache/

# Tauri desktop shell build artifacts.
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/binaries/

# Design mock-up reference (local, not part of repo)
.design-ref/
*.cpuprofile

.probe-snapshots/
.reasonix-harness-inspect/

# Local per-user Reasonix state at any depth (logs, truncated results, scratch).
# This also hides team-level settings and skills inside every .reasonix directory.
**/.reasonix/

cookingforblockheads/

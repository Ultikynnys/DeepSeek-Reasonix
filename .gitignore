node_modules/
dist/
coverage/
.stryker-tmp/
.env
.env.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/
*.tsbuildinfo
sessions/
*.jsonl
!tests/**/*.jsonl
.turbo/
# Local-only Claude Code settings — personal overrides, never committed.
.claude/settings.local.json
# Per-user Reasonix state under .reasonix/ — fully ignored (see the blanket
# `.reasonix/` rule at the bottom). Force-add settings.json / skills/ if you
# ever want them tracked.
# Personal bun lockfile — project uses npm officially.
bun.lock

# Local portable Node/npm used for development on machines without npm.
.tools/
.npm-cache/

# Tauri desktop shell build artifacts.
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/binaries/

# Design mock-up reference (local, not part of repo)
.design-ref/
*.cpuprofile

.probe-snapshots/

# Local per-user Reasonix state (logs, truncated results, scratch). Note: this
# also hides team-level .reasonix/settings.json and .reasonix/skills/ — if you
# want those tracked, force-add them (git add -f) or add explicit !-renegades.
.reasonix/
